# Experiment Archives

The complete data and log snapshots are attached to the GitHub release `recovery-v1`:

- `datasets.tar.gz` restores the `dataset/` directory.
- `logs.tar.gz` restores all retained experiment logs under `logs/`.

Download both assets from the release page, verify them with `SHA256SUMS`, and extract them from the repository root.
